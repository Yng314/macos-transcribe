- 当前正在做什么：正在重新生成并发布修复后的签名公证版安装包，确保朋友机器上不会再因资源路径问题启动即崩。
- 上次停在哪个位置：已经确认崩溃根因是打包后的 app 仍走了 `Bundle.module` 的资源包查找路径，而朋友机器没有开发时的 `.build` 回退目录。
- 近期的关键决定和原因：菜单栏图标直接改为从主 app bundle 资源读取，同时 DMG 校验改用 `stapler validate`，避免把已公证的 DMG 误判成失败。
