- 当前正在做什么：正在修复朋友机器上启动即崩的问题，根因是菜单栏图标仍然依赖 SwiftPM 资源包查找路径。
- 上次停在哪个位置：朋友端通过终端直接运行后，已经确认崩溃发生在 `resource_bundle_accessor.swift` 的资源包加载阶段。
- 近期的关键决定和原因：菜单栏图标改为直接从主 app bundle 的 `Contents/Resources` 读取，不再依赖会回落到开发机 `.build` 路径的 `Bundle.module` 逻辑。
